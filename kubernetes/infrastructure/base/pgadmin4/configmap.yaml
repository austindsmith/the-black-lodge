apiVersion: v1
kind: ConfigMap
metadata:
  name: pgadmin4
data:
  servers.json: |
    {
      "Servers": {
        "1": {
          "Name": "cloudnative-pg",
          "Group": "Servers",
          "Host": "cloudnative-pg-rw",
          "Port": 5432,
          "MaintenanceDB": "postgres",
          "Username": "postgres",
          "PassFile": "/pgadmin4/pgpass/file.pgpass",
          "SSLMode": "prefer"
        }
      }
    }
