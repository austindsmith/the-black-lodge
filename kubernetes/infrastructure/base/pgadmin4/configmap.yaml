apiVersion: v1
kind: ConfigMap
metadata:
  name: pgadmin4
data:
  servers.json: |
    {
      "Servers": {
        "1": {
          "Name": "cloudnative-pg",
          "Group": "Servers",
          "Host": "postgres-cluster-rw.postgres.svc.cluster.local",
          "Port": 5432,
          "MaintenanceDB": "postgres",
          "Username": "postgres",
          "PassFile": "/pgpassfile",
          "SSLMode": "prefer"
        }
      }
    }
