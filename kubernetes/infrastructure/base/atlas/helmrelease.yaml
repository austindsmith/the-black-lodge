apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: atlas-operator
  namespace: cloudnative-pg
spec:
  interval: 1h
  chart:
    spec:
      chart: atlas-operator
      version: ">=0.7.0"
      sourceRef:
        kind: HelmRepository
        name: ariga
        namespace: cloudnative-pg
  values:
    prewarmDevDB: false