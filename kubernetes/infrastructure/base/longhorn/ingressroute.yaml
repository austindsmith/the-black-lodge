apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: longhorn
  namespace: traefik
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`longhorn.theblacklodge.dev`)
      services:
        - name: longhorn-frontend
          #kind: TraefikService