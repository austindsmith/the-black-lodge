apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: semaphore-ingress
  namespace: traefik
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`semaphore.theblacklodge.dev`)
      kind: Rule
      services:
        - name: semaphore
          port: 80
