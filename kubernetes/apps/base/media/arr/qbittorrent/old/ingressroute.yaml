apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: qbittorrent-ingress
  namespace: media
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`qbittorrent.theblacklodge.dev`)
      kind: Rule
      services:
        - name: qbittorrent
          port: 8080
