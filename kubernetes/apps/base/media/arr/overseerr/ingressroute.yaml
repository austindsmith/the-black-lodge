apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: overserr-ingress
  namespace: traefik
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`overserr.theblacklodge.dev`)
      kind: Rule
      services:
        - name: overserr
          port: 5055
