apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: plex
spec:
  install:
    createNamespace: true
  interval: 1h
  chart:
    spec:
      chart: plex
      version: 2.1.1
      sourceRef:
        kind: HelmRepository
        name: plex
