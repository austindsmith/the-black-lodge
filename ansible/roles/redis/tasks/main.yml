- name: Install packages
  ansible.builtin.dnf:
    name:
      - valkey
    state: present
    update_cache: true

- name: Start and enable services
  ansible.builtin.service:
    name: valkey
    state: started
    enabled: true
