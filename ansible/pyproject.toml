[project]
name = "ansible"
version = "0.1.0"
description = "Ansible playbooks, inventories and roles"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ansible-core>=2.20.3",
    "ansible-lint>=26.3.0",
]
