all:
  children:
    servers:
      children:
        pihole_servers:
            hosts:
%{ for name, node in nodes ~}
                ${name}:
                    ansible_host: ${split("/", node.ip)[0]}
%{ endfor ~}
